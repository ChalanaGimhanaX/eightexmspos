<UserControl x:Class="Enightx.Pos.Wpf.Views.ReceivingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000" Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryDarkBrush}" Padding="16,8">
            <DockPanel>
                <TextBlock Text="SUPPLIER GOODS RECEIVING &amp; GRN" FontWeight="Bold" FontSize="18" Foreground="White" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="← Back to POS (F1)" Height="34" Padding="14,2" Background="#1E88E5" Foreground="White"
                            FontWeight="Bold" Click="BackToPos_Click" Margin="0,0,12,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Document Details Card -->
            <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="16" CornerRadius="6"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="Supplier:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <ComboBox x:Name="SupplierCombo" Height="36" SelectedIndex="0">
                            <ComboBoxItem Content="SUP001 - Toyota Lanka Ltd"/>
                            <ComboBoxItem Content="SUP002 - Denso International"/>
                            <ComboBoxItem Content="SUP003 - Mobil Lubricants"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="0,0,12,0">
                        <TextBlock Text="Purchase Order Number:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox x:Name="PoNumberInput" Height="36" Text="PO-2026-001" Padding="6,2" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Supplier Invoice Number:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox x:Name="SupplierInvoiceInput" Height="36" Text="INV-78901" Padding="6,2" VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Line Items Entry & Grid -->
            <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" Padding="16" CornerRadius="6"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,12">
                <DockPanel>
                    <!-- Add Line Item Row -->
                    <Border DockPanel.Dock="Top" Background="#F5F7FA" Padding="12" CornerRadius="4" Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="Product:" FontSize="12" Margin="0,0,0,2"/>
                                <ComboBox x:Name="ProductCombo" Height="34" DisplayMemberPath="Name" SelectedValuePath="ProductId"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="0,0,8,0">
                                <TextBlock Text="Qty Received:" FontSize="12" Margin="0,0,0,2"/>
                                <TextBox x:Name="QtyInput" Height="34" Text="10" Padding="6,2" VerticalContentAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Margin="0,0,8,0">
                                <TextBlock Text="Unit Cost (LKR):" FontSize="12" Margin="0,0,0,2"/>
                                <TextBox x:Name="UnitCostInput" Height="34" Text="3200.00" Padding="6,2" VerticalContentAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Grid.Column="3" Margin="0,0,8,0">
                                <TextBlock Text="Batch Number:" FontSize="12" Margin="0,0,0,2"/>
                                <TextBox x:Name="BatchInput" Height="34" Text="BAT-202609" Padding="6,2" VerticalContentAlignment="Center"/>
                            </StackPanel>
                            <Button Grid.Column="4" Content="+ Add Line" Height="34" Padding="14,2" Margin="0,16,0,0"
                                    Background="{StaticResource PrimaryBrush}" Foreground="White" FontWeight="Bold"
                                    Click="AddLine_Click"/>
                        </Grid>
                    </Border>

                    <!-- GRN Items Grid -->
                    <DataGrid x:Name="GrnItemsGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                              Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, StringFormat={}{0:F0}}" Width="80" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Unit Cost" Binding="{Binding UnitCost, StringFormat=LKR {0:F2}}" Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Line Total" Binding="{Binding LineTotal, StringFormat=LKR {0:F2}}" Width="130" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Batch" Binding="{Binding BatchNumber}" Width="120" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <!-- Bottom Summary & Commit -->
            <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" Padding="16" CornerRadius="6"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <DockPanel>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="GrnSummaryText" Text="GRN Lines: 0 | Total Units: 0" FontWeight="SemiBold" FontSize="14"/>
                        <TextBlock Text="Inventory Valuation: Moving Weighted Average Cost basis will update automatically upon commit." 
                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="TOTAL COST: " FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock x:Name="TotalCostText" Text="LKR 0.00" FontWeight="Bold" FontSize="20" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center" Margin="0,0,24,0"/>
                        <Button Content="Commit &amp; Post GRN" Height="42" Padding="20,2" Background="{StaticResource AccentBrush}" Foreground="White"
                                FontWeight="Bold" FontSize="16" Click="CommitGrn_Click"/>
                    </StackPanel>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

