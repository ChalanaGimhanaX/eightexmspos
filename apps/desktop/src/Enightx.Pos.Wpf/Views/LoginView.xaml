<UserControl x:Class="Enightx.Pos.Wpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Border Background="{StaticResource SurfaceBrush}"
                Width="420" Padding="32" CornerRadius="8"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="ENIGHTX POS" FontSize="26" FontWeight="Bold" 
                           Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="Terminal Login" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" Margin="0,4,0,24"/>

                <TextBlock Text="Username" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBox x:Name="UsernameBox" Height="36" Padding="8,4" FontSize="14"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Password" FontWeight="SemiBold" Margin="0,16,0,4"/>
                <PasswordBox x:Name="PasswordBox" Height="36" Padding="8,4" FontSize="14"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" FontSize="12" 
                           Margin="0,12,0,0" TextWrapping="Wrap"/>

                <Button Content="Sign In" Height="42" Margin="0,24,0,0"
                        Background="{StaticResource PrimaryBrush}" Foreground="White"
                        FontWeight="Bold" FontSize="15" Click="SignIn_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
